pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "WOrkspace is:: ${WORKSPACE}"
                sh 'echo "Hello World"'
                sh '''
                    echo "Multiline shell steps works too..ddd"
                    ls -lah
                '''
                       script {
                           echo "inside"
                        tempSH = "${WORKSPACE}/temp.sh"
                       
                       echo "tempSH ${tempSH}"
                       sh "${tempSH}"
                      }

            }
        }
    }
}
